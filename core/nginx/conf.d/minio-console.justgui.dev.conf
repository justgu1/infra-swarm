# minio-console.justgui.dev — MinIO Console UI
server {
    listen 80;
    server_name minio-console.justgui.dev;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://minio-console.justgui.dev$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name minio-console.justgui.dev;

    ssl_certificate     /etc/letsencrypt/live/minio-console.justgui.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/minio-console.justgui.dev/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://storage_minio:9001;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
    }
}
