# hericarealtor.com — Laravel Octane
server {
    listen 80;
    server_name hericarealtor.com www.hericarealtor.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://hericarealtor.com$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name hericarealtor.com www.hericarealtor.com;

    ssl_certificate     /etc/letsencrypt/live/hericarealtor.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/hericarealtor.com/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://hericarealtor_app:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_read_timeout 300;
    }
}
